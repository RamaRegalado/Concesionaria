/*Etapa 1
En esta primera etapa, necesitamos requerir tu módulo autos 
que se encuentra en la misma carpeta del archivo donde estás trabajando.

Además, necesitarás crear un objeto literal llamado concesionaria 
que contendrá todas las funcionalidades que el cliente solicita.

Por último, nuestro objeto literal debe tener un atributo llamado autos 
que contenga la lista de automóviles importada anteriormente.*/

/* requerir módulo autos */

//Recordá que la ruta "." representa al directorio actual.
// Por ejemplo, para acceder al archivo juegos del directorio actual la ruta sería "./juegos".


const autos = require("./autos(AlFinEsTiempoDeCoder(1)) OK");
const concesionaria = {
       autos: autos};

console.log(concesionaria);